redScreen = class()

function redScreen:init(x)
    -- you can accept and set parameters here
    self.x = x
end

function redScreen:draw()
    -- Codea does not automatically call this method
end

function redScreen:touched(touch)
    -- Codea does not automatically call this method
end
